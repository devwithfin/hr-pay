info:
  name: Get By ID Overtime Rate
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/overtime-rates/1"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

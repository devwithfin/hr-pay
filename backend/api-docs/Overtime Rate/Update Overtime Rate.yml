info:
  name: Update Overtime Rate
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/overtime-rates/1"
  body:
    type: json
    data: |-
      {
        "rate_type" : "Test Data",
        "multiplier" : "2",
        "description" : "Test Desc"
      }
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

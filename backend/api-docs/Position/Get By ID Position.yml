info:
  name: Get By ID Position
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/positions/15"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

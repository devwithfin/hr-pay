info:
  name: Get By ID Department
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/departments/12"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

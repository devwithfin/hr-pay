info:
  name: Delete Department
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/departments/11"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Create Department
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/departments"
  body:
    type: json
    data: |-
      {
        "department_name" : "Deppartment Tst"
      }
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

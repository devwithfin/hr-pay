info:
  name: Update Department
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/departments/11"
  body:
    type: json
    data: |-
      {
        "department_name" : "Department Test"
      }
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

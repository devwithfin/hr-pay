info:
  name: Delete Deduction
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/deductions/1"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

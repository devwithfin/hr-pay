info:
  name: Update Deduction
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/deductions/1"
  body:
    type: json
    data: |-
      {
        "deduction_name" : "Deduction Test"
      }
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

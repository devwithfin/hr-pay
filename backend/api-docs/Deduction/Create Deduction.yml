info:
  name: Create Deduction
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/deductions"
  body:
    type: json
    data: |-
      {
        "deduction_name" : "Deduction Tst"
      }
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get By ID Allowance
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/allowances/4"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

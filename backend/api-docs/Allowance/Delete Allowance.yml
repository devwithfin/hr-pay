info:
  name: Delete Allowance
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/allowances/4"
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
